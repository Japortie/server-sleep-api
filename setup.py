from setuptools import setup

setup(name='server-sleep-api',
      version='0.1',
      description='This is the API for server-sleep plugins.',
      url='https://github.com/SLCoding/server-sleep-api',
      author='SourceLan',
      author_email='schuette.marcus@googlemail.com',
      license='MIT',
      packages=['server_sleep_api'],
      install_requires=[
      ],
      zip_safe=False)
